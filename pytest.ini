[pytest]
# 该环境装了 ROS 的 pytest 插件（launch_testing / launch_testing_ros_pytest_entrypoint），
# 它们会自动加载并因缺失依赖或未知 hook 导致 `pytest` 直接崩溃。这里显式禁用这些
# 与本项目无关的插件，让项目自身的测试可开箱运行（不影响项目逻辑）。
addopts = -p no:launch_testing -p no:launch_ros
testpaths = tests
